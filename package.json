{
  "name": "hlolli-github-io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14"
  },
  "scripts": {
    "dev": "bun run build && bun scripts/serve.ts",
    "test": "bun install --cwd tools/plugin-compiler --frozen-lockfile && bun install --cwd tools/lilypond-wasm/editor --frozen-lockfile && bun test --cwd tools/plugin-compiler && bun test --cwd tools/lilypond-wasm/editor --path-ignore-patterns='dist/**'",
    "build": "bun scripts/build.ts",
    "preview": "bun scripts/serve.ts"
  }
}
